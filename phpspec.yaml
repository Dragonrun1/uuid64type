code_generation: false
stop-on-failure: true
formatter.name: pretty
suites:
  default:
    namespace: Uuid64Type
    psr4_prefix: Uuid64Type
    spec_path: "%paths.config%/spec"
    spec_prefix: Spec
    src_path: "%paths.config%/src"
